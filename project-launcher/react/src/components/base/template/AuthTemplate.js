import React from 'react';

/**
 * 
 * @param {*} param0 ReactElement
 */
function AuthTemplate({children = null}) {
  return (
    <div>
      {children}
    </div>
  );
}

export default AuthTemplate;